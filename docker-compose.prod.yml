# Production Docker Compose — RegEngine on Railway + Supabase
#
# NOTE: Production runs on Vercel (frontend) + Railway (backend) + Supabase (database).
# This file is provided for self-hosted or on-prem deployments.
# It does NOT include Neo4j, Kafka, or Redis — those are legacy services
# from the original distributed design and are not in the production critical path.
#
# For the full local dev stack (including legacy services), use docker-compose.yml.

x-env-prod: &env_prod
  REGENGINE_ENV: production
  DATABASE_URL: ${DATABASE_URL:?error}
  ADMIN_DATABASE_URL: ${ADMIN_DATABASE_URL:?error}
  LOG_LEVEL: ${LOG_LEVEL:-WARNING}
  ENABLE_DB_API_KEYS: "true"

x-deploy-defaults: &deploy_defaults
  restart_policy:
    condition: on-failure
    delay: 5s
    max_attempts: 3

services:
  admin-api:
    build:
      context: .
      dockerfile: ./services/admin/Dockerfile
    image: ghcr.io/regengine/admin-api:${IMAGE_TAG:-latest}
    environment:
      <<: *env_prod
      ADMIN_MASTER_KEY: ${ADMIN_MASTER_KEY:?error}
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8400/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      <<: *deploy_defaults
      resources:
        limits:
          cpus: '1.0'
          memory: 1G

  ingestion-service:
    build:
      context: .
      dockerfile: ./services/ingestion/Dockerfile
    image: ghcr.io/regengine/ingestion-service:${IMAGE_TAG:-latest}
    environment:
      <<: *env_prod
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      <<: *deploy_defaults
      resources:
        limits:
          cpus: '2.0'
          memory: 2G

  compliance-api:
    build:
      context: .
      dockerfile: ./services/compliance/Dockerfile
    image: ghcr.io/regengine/compliance-api:${IMAGE_TAG:-latest}
    environment:
      <<: *env_prod
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8500/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      <<: *deploy_defaults
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
