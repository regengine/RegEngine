For the current task:

1. Read `AGENTS.md` and confirm the real target files.
2. Inspect nearby code before proposing changes.
3. Produce a short implementation plan with files, risks, and tests.
4. Make only the minimum code changes needed.
5. Run the narrowest matching verification commands.
6. Return a final summary with changed files, commands run, and any blocked verification.

Reject stale assumptions such as missing paths, missing Make targets, or absolute local file links.
