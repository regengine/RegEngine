/**
 * RegEngine Design System — Barrel Export
 *
 * Usage: import { colors, spacing, shadows, verticalColors } from '@/design-system'
 */

export {
    colors,
    spacing,
    typography,
    shadows,
    radii,
    transitions,
    verticalColors,
} from './tokens'

export type {
    StatusColor,
    AccentColor,
    VerticalKey,
    FontSize,
} from './tokens'
