[build]
builder = "DOCKERFILE"
dockerfilePath = "./services/graph/Dockerfile"
watchPatterns = ["services/graph/**", "services/shared/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
