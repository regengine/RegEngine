[build]
builder = "DOCKERFILE"
dockerfilePath = "./services/scheduler/Dockerfile"
watchPatterns = ["services/scheduler/**", "services/shared/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
