[build]
builder = "DOCKERFILE"
dockerfilePath = "./services/ingestion/Dockerfile"
watchPatterns = ["services/ingestion/**", "services/shared/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
