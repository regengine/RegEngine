# DEPRECATED — use root requirements.txt (single source of truth)
# This file is retained for backward compatibility only. Do not add deps here.
# See pyproject.toml for canonical dependency specifications.
# Minimal local test bootstrap for ingestion API route tests.
# This avoids installing full production dependencies when running
# focused endpoint tests in services/ingestion/tests.

fastapi>=0.136.1
pydantic>=2.13.3
pydantic-settings>=2.14.0
httpx>=0.28.1
python-dotenv>=1.2.2
pytest>=9.0.3
pytest-asyncio>=1.3.0
